# GoWay

Starter repo (Next.js + Firebase) with Terraform & Kubernetes scaffolding.
Replace Firebase and AWS values before deployment.

## Local dev
1. `npm install`
2. add Firebase config in `firebase.js`
3. `npm run dev`
Open http://localhost:3000

